# Web Development

- Inspect the existing architecture before changing it.
- Keep frontend and backend responsibilities clear.
- Validate input and handle errors without exposing internal details.
- Use environment variables for configuration.
- Keep secrets in backend environment variables; never expose them to frontend code.
- Preserve mobile usability and use semantic, accessibility-friendly HTML.
- Avoid unnecessary libraries and keep components reasonably small.
